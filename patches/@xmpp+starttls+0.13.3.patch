diff --git a/node_modules/@xmpp/starttls/client.js b/node_modules/@xmpp/starttls/client.js
index ec71bc7..349294b 100644
--- a/node_modules/@xmpp/starttls/client.js
+++ b/node_modules/@xmpp/starttls/client.js
@@ -27,7 +27,10 @@ module.exports = function starttls({ streamFeatures }) {
     }
 
     await negotiate(entity);
-    const tlsSocket = await upgrade(socket, { host: options.domain });
+    const tlsSocket = await upgrade(socket, {
+        host: options.domain,
+        rejectUnauthorized: options.rejectUnauthorized,
+    });
     entity._attachSocket(tlsSocket);
 
     await entity.restart();
